#include <iostream>
#include <queue>
#include <cstdio>
#include <map>
#include <algorithm>

using namespace std;

typedef long long ll;

const int maxn = 150010;
ll aray[maxn];
int n;

int main() {
	scanf("%d", &n);
	for (int i = 1; i <= n; i++)
		scanf("%lld", &aray[i]);
}