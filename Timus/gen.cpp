#include <bits/stdc++.h>

using namespace std;

int main() {
  freopen("read.in", "w", stdout);
  srand(time(NULL));
  int n = rand() % 44 + 1, k = rand() % (int)1e9;
  cout << n << ' ' << k << endl;
  return 0;
}
